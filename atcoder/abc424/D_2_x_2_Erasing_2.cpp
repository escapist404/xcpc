#include <bits/stdc++.h>

void solve() {
    int h, w;
    std::cin >> h >> w;
    std::vector<std::string> m(h);
    for (int i = 0; i < h; ++i) {
        std::cin >> m[i];
    }

        
}

int main() {
    int tt;
    std::cin >> tt;
    while (tt--) {
        solve();
    }

    return 0;
}