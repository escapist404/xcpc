#include <bits/stdc++.h>

void solve() {
    int n;
    std::cin >> n;
    std::cout << (n - 1) / 2 << "\n";
}

int main() {
    std::cin.tie(nullptr);
    std::ios::sync_with_stdio(false);

    int T;
    std::cin >> T;

    while (T--) {
        solve();
    }

    return 0;
}