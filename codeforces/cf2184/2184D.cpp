#include <vector>
#include <iostream>
#include <algorithm>
#include <numeric>

void solve() {
    int n, k;
    std::cin >> n >> k;

    int d = std::__lg(n);
    std::vector<int> f(1);

    
}

int main() {
    int tt;
    std::cin >> tt;

    while (tt--) {
        solve();
    }

    return 0;
}
