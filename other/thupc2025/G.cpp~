#include <bits/stdc++.h>

void solve() {
  int n;
  std::cin >> n;

  std::string s;
  std::cin >> s;

  int p = 0;
  for (int i = 0; i < n; ++i) {
    if (s[i] == '1') {
      
    }
  }
}

int main() {
  int T;
  std::cin >> T;
  while (T--) {
    solve();
  }
}
